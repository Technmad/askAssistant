# Frontend

Next.js chat UI for the AI Personal Assistant. See the [project root README](../README.md) for architecture, setup, and everything else.

Quick start:

```bash
npm install
cp .env.example .env.local
npm run dev
```

Open http://localhost:3000.
